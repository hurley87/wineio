<% @wines.each do |wine| %>
	<%= link_to wine_path(wine) do %><div class="box">
		<h3><%= wine.name %></h3>
		<p><%= wine.location  %></p>
		<p class='center'><img src="<%= wine.image %>"></p>
	</div><% end %>
<% end %>