

<h1 class='opener center'><%= @user.name %>'s Wine Rack</h1>
<h3 class='opener center'><%= link_to 'Edit Profile', edit_user_path(@user), class: "o-link" %></h3>

<div id="wine-listing" class="transitions-enabled">
	<%= render 'wines/wine_list' %>
</div>